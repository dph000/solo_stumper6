/*
** EPITECH PROJECT, 2021
** luhn
** File description:
** main function
*/

#include "luhn.h"

int main(int ac, char **av)
{
    if (ac != 2)
        return (84);
    if (my_strisnum(av[1]) == 0)
        return (84);
    my_putstr("valid\n");
    return (0);
}
